# Harvest Report: 2026-06-09

## Metadata

- **Command**: `/devspark.harvest`
- **Date**: 2026-06-09
- **Scope**: full
- **Release Context**: v1.4.0 completed and published
- **Scanner**: `.devspark/scripts/bash/harvest.sh --json`

## Summary

Post-release harvest completed for the v1.4.0 cleanup cycle.

The scanner reported no active completed specs under `.documentation/specs/`. The v1.4.0 release already moved `001-remote-api-list` into `.documentation/releases/v1.4.0/specs/001-remote-api-list/`, so no spec archival was needed during harvest.

## Knowledge Preserved

Durable v1.4.0 knowledge was already present in living and release documents before harvest:

- `CHANGELOG.md` contains the v1.4.0 release entry.
- `README.md`, `ApiTestSpark/README.md`, `NUGET-PACKAGE-WALKTHROUGH.md`, and `DEPLOYMENT.md` document Remote API Profiles and the v1.4.0 release workflow.
- `.documentation/decisions/ADR-009.md` records the server-profile-only proxy decision.
- `.documentation/releases/v1.4.0/release-notes.md` and `metrics.json` preserve release metadata.
- `.documentation/repo-story/` current-version FAQ entries now point at v1.4.0.

## Code Comment Cleanup

Harvest rewrote spec/task-linked comments in `src/components/host-api/EndpointTester.tsx` into self-contained comments.

Removed tracking markers included:

- `T005`, `T006`, `T007-T013`, `T010`, `T011`, `T014`, `T015`, `T018-T020`, `T029`, `T030`, `T033a`
- `FR-001`, `FR-002`, `FR-003`, `FR-007`, `FR-012`
- `critic-001`, `critic-002`, `critic-005`
- `M-01`
- `US5`
- `data-model.md`

The resulting comments now describe lasting behavior:

- local table expansion starts compact for each response
- nested response edits are merged when copying JSON
- circular and recursive response shapes are guarded
- null and undefined fields render read-only where type recovery is ambiguous
- arrays and deeper objects choose table or read-only JSON renderers
- JSON view mode is read from the session store
- response-panel cURL is captured only after successful calls
- response remounting uses an O(1) counter instead of deriving keys from JSON

## Archived Files

No files were moved to `.archive/`.

The following scanner candidates were intentionally kept:

- `.documentation/releases/**`: versioned historical records produced by `/devspark.release`
- `.documentation/specs/pr-review/pr-2.md` and `pr-3.md`: completed PR reviews, but not older than the 30-day retention threshold
- `.documentation/copilot/audit/2026-05-28_results.md` and `2026-05-31_results.md`: completed audits, but not older than the 60-day retention threshold

## Notes

The Bash harvest scanner emitted malformed JSON for empty arrays (`specs`, `stale_drafts`, `session_notes`, and related fields). The useful inventory still identified documentation and code-comment candidates, and the harvest plan was executed conservatively from that inventory.

## Validation

- Spec-linked marker scan for `src/components/host-api/EndpointTester.tsx`: clean
- Markdownlint: pending after report creation
